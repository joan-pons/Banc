package es.bancodehierro.banco.tarjeta;

import es.bancodehierro.banco.cc.CuentaCorriente;
import es.bancodehierro.banco.persona.Cliente;

public class GestionTarjetas {

    public Boolean altaTarjeta(int codigoTarjeta, Cliente titular, CuentaCorriente cuentacorreiente, TipoTarjeta tipotarjeta, Double limite) {
        if(limite==null){
            Debito d = new Debito();
        } else {
            
        }
        return null;
    }
}
